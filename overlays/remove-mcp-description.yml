overlay: 1.0.0
info:
  title: Remove description field from x-speakeasy-mcp objects
  version: 1.0.0

actions:
  # Remove description field from x-speakeasy-mcp objects
  - target: "$.paths.*.*['x-speakeasy-mcp'][?(@.description)].description"
    remove: true 